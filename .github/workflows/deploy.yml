# name: 自动打包并部署

# on:
#   push:
#     branches:
#       - master

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout 代码
#         uses: actions/checkout@v2

#       - name: Install pnpm # 安装pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: 6.0.2

#       - name: 安装依赖
#         run: pnpm install

#       - name: 构建应用
#         run: pnpm docs:build

#       - name: 部署到服务器
#         uses: easingthemes/ssh-deploy@v2.2.6
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }} # SSH 私钥，用于与远程服务器建立安全连接。
#           ARGS: '-rltgoDzvO --delete'
#           SOURCE: 'dist/' # 要部署的文件或目录的路径。
#           REMOTE_HOST: ${{ secrets.REMOTE_HOST }} # 远程服务器的主机名或 IP 地址。
#           REMOTE_USER: ${{ secrets.REMOTE_USER }} # 连接远程服务器所需的用户名
#           # TARGET: '/var/www/my-app' # 部署到远程服务器的目标路径

# name: 自动化部署

# on:
#   push:
#     branches:
#       - master

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout 代码
#         uses: actions/checkout@v2

#       - name: Install pnpm # 安装pnpm
#         uses: pnpm/action-setup@v2
#         with:
#           version: 6.0.2

#       - name: 构建网站
#         run: |
#           pnpm install
#           pnpm  docs:build

#       # - name: 部署到 GitHub Pages
#       #   uses: peaceiris/actions-gh-pages@v3
#       #   with:
#       #     github_token: ${{ secrets.GITHUB_TOKEN }}
#       #     publish_dir: ./dist
#       - name: Deploy # 部署
#         uses: JamesIves/github-pages-deploy-action@v4.3.3
#         with:
#           branch: gh-pages # 部署后提交到的分支
#           folder: dist # 这里填打包好的目录名称

name: 自动化部署
on: # 监听 main 分支上的 push 事件
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # 构建环境使用 ubuntu
    steps:
      - name: Checkout # 将代码拉到虚拟机
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install pnpm # 安装pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 6.0.2

      - name: Install and Build # 下载依赖 打包项目
        run: |
          npm install
          npm run build

      - name: Deploy # 部署
        uses: JamesIves/github-pages-deploy-action@v4.3.3
        with:
          branch: gh-pages # 部署后提交到的分支
          folder: dist # 这里填打包好的目录名称
